// src/lib/design/tokens.ts
export const colors = {
  moneyIn: "hsl(var(--money-in))", // Vert (Entrées)
  moneyOut: "hsl(var(--money-out))", // Violet/Indigo Thrive (Sorties/Primaire)
  danger: "hsl(var(--danger))", // Rouge (Erreurs)
  commission: "hsl(var(--commission))", // Violet doux (Commission Réparto)
  accentDecorative: "hsl(var(--accent-decorative))", // Orange Thrive (Décoratif uniquement)
  moneyOutSubtle: "hsl(var(--money-out-subtle))", // Item actif, fonds clairs
};

export const formatting = {
  currency: "XOF",
  locale: "fr-FR",
};
